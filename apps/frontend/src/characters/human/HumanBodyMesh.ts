/**
 * Shared character role + future SkinnedMesh body entry points.
 * `CharacterRole` is the single source of truth for role typing across the human pipeline.
 */
export type CharacterRole = 'batsman' | 'bowler' | 'fielder' | 'keeper';
