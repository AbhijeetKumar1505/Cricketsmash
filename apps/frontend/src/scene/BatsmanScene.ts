/**
 * BatsmanScene — hero character panel for the reactive right-side display.
 * Thin re-export of CharacterPanel so the scene/ folder matches the declared architecture.
 */

export { CharacterPanel as BatsmanScene } from './CharacterPanel.js';
export type { PlayerId } from '../game/CharacterManager.js';
